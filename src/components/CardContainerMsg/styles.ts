import styled from 'styled-components'

export const InitialMsg = styled.span`
  display: none; // Mudar
  color: #fff;
  font-size: 22px;
  text-align: center;
  margin-top: 38px;
  margin-right: 32px;
`
